<nav class="navbar navbar-expand-md border-bottom sticky-top">
    <div class="container-fluid">
        <a class="navbar-brand" href="/"><i class="fa-regular fa-compass"></i></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <a class="nav-link" href="/"><i class="fa-solid fa-house"></i> Home</a>
                <a class="nav-link" href="/listings"><i class="fa-solid fa-list"></i> All Listings</a>
                <a class="nav-link" href="/listings/new"><i class="fa-solid fa-user-plus"></i> Add new Listing</a>
            </div>

            <div class="navbar-nav ms-auto">
                <% if (!currUser) { %>
                    <a class="nav-link" href="/signup">Signup</a>
                    <a class="nav-link" href="/login">Login</a>
                <% } %>
                <% if (currUser) { %>
                    <a class="nav-link" href="/logout">Logout</a>
                <% } %>
            </div>
        </div>
    </div>
</nav>

<!-- JavaScript to change navbar background on nav-link hover
<script>
    const navbar = document.getElementById('navbar');
    const navLinks = document.querySelectorAll('.nav-link');

    navLinks.forEach(link => {
        link.addEventListener('mouseover', () => {
            navbar.style.background = 'linear-gradient(90.2deg, #130F40 0%, rgb(186, 186, 186) 50%, rgba(255, 255, 255, 0.93) 100%)'; // Change to desired color on hover
        });
        link.addEventListener('mouseout', () => {
            navbar.style.background = 'linear-gradient(90.2deg, rgba(255, 255, 255, 0.93) 0%, rgb(186, 186, 186) 50%, #130F40 100%)'; // Reset to original background
        });
    });
</script> -->


